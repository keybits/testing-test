machine:
  python:
    version: 2.7.10

test:
  override:
    - python flaskr_test.py:
      parallel: true 
  post:
    - touch $CIRCLE_ARTIFACTS/file.txt

deployment:
  release:
    tag: /v[0-9]+/
    owner: keybits
    commands:
      - "true"