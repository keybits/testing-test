machine:
  python:
    version: 2.7.10

general:
  branches:
    ignore:
      - master

test:
  post:
    - touch $CIRCLE_ARTIFACTS/file.txt

deployment:
  release:
    tag: /\d+\.\d+\.\d+/
    owner: keybits
    commands:
      - "true"