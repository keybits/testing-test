machine:
  python:
    version: 2.7.10
  services:
    - redis

general:
  branches:
    only:
      - master
      - /v[0-9]+/

test:
  post:
    - touch $CIRCLE_ARTIFACTS/file.txt

deployment:
  release:
    tag: /v[0-9]+/
    owner: keybits
    commands:
      - "true"