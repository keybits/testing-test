machine:
  python:
    version: 2.7.10

test:
  override:
    - nosetests:
        parallel: true 
  post:
    - touch $CIRCLE_ARTIFACTS/file.txt

deployment:
  release:
    tag: /v[0-9]+/
    owner: keybits
    commands:
      - "true"